window.addEventListener("DOMContentLoaded", () => {
    // const tooltip = document.querySelector(".stats-section .tooltip");
    // const aside = document.querySelector(".stats-section aside.info");

    // tooltip.addEventListener("mouseenter", (ev) => {
    //     aside.classList.remove("hidden");
    // });

    // tooltip.addEventListener("mouseout", (ev) => {
    //     aside.classList.add("hidden");
    // });
});